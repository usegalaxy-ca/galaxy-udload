[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "galaxy-udload"
version = "0.2.0"
description = "File utility to upload or download file from or to UseGalaxy"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "Charles Coulombe" },
    { name = "Carol Gauthier" },
]

dependencies = [
    "bioblend",
    "python-dotenv",
    "rich",
]

[project.scripts]
galaxy-upload = "galaxy_udload.upload:main"
galaxy-download = "galaxy_udload.download:main"
galaxy-history = "galaxy_udload.history:main"

[tool.setuptools]
packages = ["galaxy_udload"]
